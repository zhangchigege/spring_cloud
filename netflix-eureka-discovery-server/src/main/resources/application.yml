server:
  port: 8000
spring:
  application:
    name: netflix-eureka-discovery-server
eureka:
  client:
    register-with-eureka: false
    fetch-registry: false
    service-url:
      defaultZone: http://127.0.0.1:${server.port}/eureka/
